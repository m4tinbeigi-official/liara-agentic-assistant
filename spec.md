# مشخصات فنی و عملکردی دستیار هوشمند و ایجنتیک مستندات لیارا (Liara Agentic Copilot)

## ۱. هدف و خلاصه اجرایی
این پروژه با هدف کاهش چشمگیر تیکت‌های پشتیبانی و تسریع فرآیند استقرار پروژه‌ها در لیارا طراحی شده است. سیستم فراتر از یک چت‌بات ساده متنی عمل کرده و یک **دستیار عامل‌محور (Agentic AI Copilot)** است که بر پایه کلیه مستندات متن‌باز لیارا (گیت‌هاب و وب)، خطاها و لاگ‌های کاربران را تحلیل می‌کند، فایل‌های کانفیگ اختصاصی (`liara.json`, `Dockerfile`, `.dockerignore`) تولید می‌نماید، دستورات CLI را اعتبارسنجی می‌کند و در صورت اتصال توکن، عملیات ساخت و استقرار پلتفرم را روی PaaS لیارا مدیریت می‌نماید.

---

## ۲. قابلیت‌های کلیدی و سناریوهای عامل (Agentic Features)

### الف) بازیابی دانش چندمرحله‌ای (Agentic Hybrid RAG)
- ایندکس خودکار تمام مخزن مستندات متن‌باز لیارا از گیت‌هاب (فرمت‌های Markdown و MDX).
- جستجوی ترکیبی (Hybrid Search) شامل برداری (Dense Vector) و کلیدواژه‌ای (BM25) به همراه ریرنکینگ (Re-ranking).
- ارجاع دقیق (Deep Citations) با لینک مستقیم به خطوط و سرفصل‌های داکیومنت رسمی لیارا.

### ب) عامل عیب‌یابی و تحلیل لاگ (Log & Error Diagnoser Agent)
- دریافت لاگ‌های خطای بیلد (Build Error)، خطای دیتابیس یا کرش اجرای برنامه.
- انطباق خودکار ارورها با راهنماهای رفع عیب (Troubleshooting Guides) در مستندات لیارا.
- پیشنهاد راه‌حل گام‌به‌گام به همراه فایل پچ (Diff/Fix) متناسب با زبان و فریم‌ورک پروژه (Node.js, Python, Laravel, Go, Docker و...).

### ج) سازنده تعاملی کانفیگ و دستورات CLI (Smart Configurator)
- پرسش و پاسخ تعاملی جهت تشخیص پلتفرم، نسخه Runtime، پورت ورودی و دیسک‌های لازم.
- تولید بدون نقص فایل `liara.json` و دستورات خط فرمان (`liara deploy --port=...`).

### د) اتصال به API رسمی لیارا (Interactive Cloud Operations)
- بررسی وضعیت برنامه‌ها (App Status)، سرویس‌های دیتابیس و لاگ‌های زنده با Liara API Token.
- صدور دستورات آماده جهت اجرا در Liara CLI یا اجرای مستقیم با مجوز کاربر.

---

## ۳. معماری فنی و استک نرم‌افزاری
- **Backend:** FastAPI (Python 3.11) با معماری ناهمگام (Async)، مدیریت گردش کارهای عاملی با LangGraph / LlamaIndex.
- **Vector Database:** Qdrant / ChromaDB محلی یا ابری با Embeddings پشتیبان چندزبانه (فارسی و انگلیسی).
- **Frontend UI:** Next.js 14 (App Router) + Tailwind CSS + shadcn/ui با قابلیت چت استریم، ویرایشگر کد (Monaco/CodeMirror)، و پیش‌نمایش مستقیم فایل‌های کانفیگ.
- **استقرار هدف:** پلتفرم ابری لیارا (PaaS / Docker Image) با دیسک دائمی برای وکتور دیتابیس.

---

## ۴. مدل‌های داده و ساختار اندپوینت‌های API
- `POST /api/v1/agent/chat`: دریافت پیام کاربر، نشست، و حالت استریم پاسخ به همراه ابزارهای فعال (Tool Calls).
- `POST /api/v1/agent/diagnose`: دریافت متون لاگ و فایل پروژه جهت عیب‌یابی آنی.
- `POST /api/v1/agent/generate-config`: تولید کانفیگ استاندارد لیارا متناسب با فریم‌ورک ورودی.
- `GET /api/v1/docs/search`: جستجوی مستقیم مستندات با فیلتر پلتفرم‌ها (Django, React, WordPress,...).

---

## ۵. منابع و مستندات مرجع (Reference Documentation)
برای توسعه، یکپارچه‌سازی و اتصال به سرویس‌های پلتفرم ابری لیارا، منابع زیر مبنای اعتبارسنجی و پایگاه دانش RAG قرار می‌گیرند:
- **[مستندات رسمی لیارا](https://docs.liara.ir/)**: راهنماهای استقرار، سرویس‌های ابری، پایگاه‌های داده و شبکه.
- **[مخزن GitHub مستندات لیارا](https://github.com/liara-cloud/docs)**: سورس‌کدهای Markdown و MDX جهت استخراج، چانک‌بندی و ایندکس برداری.

---

## ۶. نیازمندی‌های تحویل و خروجی نهایی (Deliverables & Submission Criteria)
خروجی نهایی پروژه برای داوری و ارزیابی باید شامل موارد زیر باشد:
1. **نسخه پایدار و فعال (Live Deployment):** ارائه لینک نسخه Deploy شده پروژه که به‌صورت کامل روی زیرساخت پلتفرم ابری لیارا در حال اجرا و تست‌پذیر باشد.
2. **سورس‌کد پروژه (GitHub Repository):** لینک ریپازیتوری GitHub شامل کلیه کدهای فرانت‌اند، بک‌اند، کانفیگ‌های `liara.json` و `Dockerfile`.
3. **قابلیت اجرای کامل:** پروژه باید به‌صورت کامل و بدون اختلال روی زیرساخت لیارا قابل اجرا و استفاده باشد.

### تبصره و پروتکل شرایط عدم امکان Deployment:
در صورتی که به هر دلیل امکان تکمیل فرآیند استقرار (Deploy) روی لیارا فراهم نشد:
- تیم می‌تواند برای ارزیابی سایر بخش‌های پروژه، یک **ویدئوی دمو از محصول** ارسال کند.
- ویدئوی دمو باید قابلیت‌های پیاده‌سازی‌شده را به‌صورت کامل نمایش دهد.
- **توجه و نحوه ارزیابی:** در این حالت، امتیاز مربوط به بخش استقرار روی زیرساخت لیارا به تیم تعلق نخواهد گرفت، اما سایر بخش‌های پروژه همچنان قابل ارزیابی خواهند بود.
