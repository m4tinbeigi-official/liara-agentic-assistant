# راهنمای وایب کدینگ و تحویل به هوش مصنوعی (Handoff Guide)

## ۱. استراتژی توسعه و پرامپت‌های مرحله‌ای (Prompting Plan)

### فاز ۱: استخراج و ایندکس مستندات لیارا (Docs Crawler & Embedding)
- **هدف:** کلون کردن ریپازیتوری مستندات لیارا از گیت‌هاب، پاک‌سازی فرانت‌مترهای MDX، چانک‌بندی هوشمند و ذخیره در وکتور استور.
- **پرامپت اجرایی:**
  > "یک اسکریپت پایتون بنویس که ریپازیتوری گیت‌هاب مستندات لیارا را بخواند، فایل‌های markdown را بر اساس سکشن‌ها چانک‌بندی کند و با مدل embedding چندزبانه در وکتور دیتابیس ایندکس نماید."

### فاز ۲: هسته عامل و معماری ابزارها (Agent Tools & LangGraph Workflow)
- **هدف:** ساخت حلقه تصمیم‌گیری عامل هوشمند با ابزارهای: `search_docs`، `diagnose_error`، `generate_liara_json`، `validate_dockerfile`.
- **پرامپت اجرایی:**
  > "با استفاده از LangGraph یک جریان تصمیم‌گیری (ReAct Agent) بساز که اگر کاربر پیام عمومی داد چت کند، اگر ارور فرستاد ابزار `diagnose_error` را صدا بزند و همواره به مستندات با لینک مستقیم ارجاع دهد."

### فاز ۳: بک‌اند FastAPI و استریم SSE (API & Streaming)
- **هدف:** پیاده‌سازی اندپوینت‌های وب‌سوکت یا Server-Sent Events برای استریم کاراکتر به کاراکتر پاسخ و Tool Callها.
- **پرامپت اجرایی:**
  > "یک سرور FastAPI طراحی کن که اندپوینت `/api/v1/agent/chat` را به صورت StreamingResponse برگرداند و وضعیت اجرای ابزارها (Searching docs, Analyzing logs) را لایو نشان دهد."

### فاز ۴: رابط کاربری مدرن و واکنش‌گرا (Next.js & UI)
- **هدف:** ساخت فرانت‌اند شیک متناسب با تم لیارا (رنگ بنفش سازمانی `#7C3AED` و دارک مود)، با قابلیت رندر کدهای رنگی و دکمه‌های کپی دستورات CLI.

### فاز ۵: داکرایز و دیپلوی روی PaaS لیارا (Docker & Liara Deployment)
- **هدف:** ایجاد `Dockerfile` چندمرحله‌ای (Multi-stage) و `liara.json` جهت استقرار بدون نقص روی سرویس PaaS لیارا.

---

## ۲. چک‌لیست تضمین استقرار، تحویل و تست داوری
- [ ] تست اجرای کامل پایپ‌لاین RAG و پاسخ‌دهی بر اساس آخرین تغییرات [مستندات رسمی لیارا](https://docs.liara.ir/) و [مخزن GitHub مستندات](https://github.com/liara-cloud/docs).
- [ ] تست سناریوی خطای رایج پورت در Node.js و تولید صحیح `PORT=3000` در `liara.json`.
- [ ] تست سناریوی خطای اتصال به دیتابیس مدیریت‌شده لیارا و ساخت URI صحیح.
- [ ] بررسی استقرار لایو روی دامنه لیارا (مثال: `https://liara-copilot.iran.liara.run`).
- [ ] آماده‌سازی و اعتبارسنجی ریپازیتوری GitHub پروژه با مستندات شفاف و کدهای کامل.
- [ ] آماده‌سازی سناریو و ضبط ویدئوی دمو کامل جهت نمایش قابلیت‌ها (به‌عنوان مستند تکمیلی یا پلن جایگزین در صورت بروز اشکال در استقرار).
